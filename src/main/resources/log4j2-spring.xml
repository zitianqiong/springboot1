<?xml version="1.0" encoding="UTF-8" standalone="no"?>

<!--设置log4j2的自身log级别为warn-->
<!--日志级别以及优先级排序: OFF > FATAL > ERROR > WARN > INFO > DEBUG > TRACE > ALL -->
<!--Configuration后面的status，这个用于设置log4j2自身内部的信息输出，可以不设置，
    当设置成trace时，你会看到log4j2内部各种详细输出-->
<!--monitorInterval：Log4j能够自动检测修改配置 文件和重新配置本身，设置间隔秒数-->
<!--  注：本配置文件的目标是将不同级别的日志输出到不同文件，最大2MB一个文件，
	文件数据达到最大值时，旧数据会被压缩并放进指定文件夹 -->
<Configuration status="WARN" name="customApp" monitorInterval="30">
	<Properties>
		<!-- 配置日志文件输出目录，此配置将日志输出到目录下的指定文件夹,防止目录遍历,末级文件夹必须为logs -->
		<Property name="LOG.PATH">./logs</Property>
		<!-- 控制台输出日志级别 -->
		<Property name="LOG.LEVEL.CONSOLE.PRINT">warn</Property>
		<Property name="LOG.LEVEL.CONSOLE.ERROR">error</Property>
		<!-- 日志输出格式 -->
		<Property name="PROJECT.NAME">springboot</Property>
		<Property name="PID">????</Property>
		<Property name="LOG_EXCEPTION_CONVERSION_WORD">%xwEx</Property>
<!--		%5p == %5level-->
		<Property name="LOG_LEVEL_PATTERN">%5p</Property>
		<Property name="LOG_TIME_FORMAT_PATTERN">H:mm:ss</Property>
		<Property name="LOG_DATEFORMAT_PATTERN">yyyy-MM-dd HH:mm:ss.SSS</Property>
<!--		如果不是确实需要，不要打印location信息，比如HTML的location，
			或者pattern模式里的%C or $class, %F or %file, %l or %location, %L or %line, %M or %method, 等，
			因为Log4j需要在打印日志的时候做一次栈的快照才能获取这些信息，这对于性能来说是个极大的损耗。-->
		<Property name="CONSOLE_LOG_PATTERN">%d{${LOG_TIME_FORMAT_PATTERN}}%clr{${LOG_LEVEL_PATTERN}} %magenta{[%5.15t]} %cyan{%30F:%-3L]:} %m%n${sys:LOG_EXCEPTION_CONVERSION_WORD}</Property>
		<Property name="FILE_LOG_PATTERN">%d{${LOG_DATEFORMAT_PATTERN}} ${LOG_LEVEL_PATTERN} ${sys:PID} --- [%t] %-40.40c{1.} :	%m%n${sys:LOG_EXCEPTION_CONVERSION_WORD}</Property>
	</Properties>

	<!--先定义所有的appender-->
	<Appenders>

		<!-- 单词解释： Match：匹配 DENY：拒绝 Mismatch：不匹配 ACCEPT：接受 -->
		<!-- DENY，日志将立即被抛弃不再经过其他过滤器； NEUTRAL，有序列表里的下个过滤器过接着处理日志； ACCEPT，日志会被立即处理，不再经过剩余过滤器。 -->
		<!--输出日志的格式 %d{yyyy-MM-dd HH:mm:ss, SSS} : 日志生产时间 %p : 日志输出格式 %c : logger的名称
			%m : 日志内容，即 logger.info("message") %n : 换行符 %C : Java类名 %L : 日志输出所在行数 %M
			: 日志输出所在方法名 -->

		<!--这个输出控制台的配置，这里输出warn和info级别的信息到System.out -->
		<!-- 异步日志将 auto-flush in batches, so switch off immediateFlush. -->
<!--		如果不是十分必须，不管是同步异步，都设置immediateFlush为false，这会对性能提升有很大帮助。-->
		<Console name="console_out_appender" target="SYSTEM_OUT" follow="true" immediateFlush="false">
			<!-- 控制台只输出level及以上级别的信息(onMatch),其他的直接拒绝(onMismatch) -->
<!--			在log4j2中，LevelRangeFilter的minLevel，maxLevel的配置是和log4j 1.x相反的；minLevel需要配置的是高级别，maxLevel配置的是低级别-->
<!--			LevelRangeFilter讲解：https://blog.csdn.net/lewky_liu/article/details/85219126-->
			<LevelRangeFilter maxLevel="info" minLevel="${LOG.LEVEL.CONSOLE.PRINT}"
				onMatch="accept" onMismatch="deny" />
			<!-- 输出日志的格式 -->
			<PatternLayout pattern="${sys:CONSOLE_LOG_PATTERN}" />
		</Console>
		<!--这个输出控制台的配置，这里输出error级别的信息到System.err，在eclipse控制台上看到的是红色文字 -->
		<Console name="console_err_appender" target="system_err" immediateFlush="false">
			<!-- 控制台只输出level及以上级别的信息(onMatch),其他的直接拒绝(onMismatch) -->
<!--			levelP>=levelQ，则levelP会打印在levelQ的log里。即：如果设置的日志级别是info，则会包括info、warn、error等高级别的日志-->
			<ThresholdFilter level="${LOG.LEVEL.CONSOLE.ERROR}"
				onMatch="accept" onMismatch="deny" />
			<!-- 输出日志的格式 -->
			<PatternLayout pattern="${sys:CONSOLE_LOG_PATTERN}" />
		</Console>

		<!-- INFO级别日志 -->
		<RollingRandomAccessFile name="info_appender" immediateFlush="false"
								 fileName="${LOG.PATH}/${PROJECT.NAME}-info.log"
			filePattern="${LOG.PATH}/${PROJECT.NAME}/info/${PROJECT.NAME}-info - %d{yyyy-MM-dd HH_mm_ss}.log.gz">
			<PatternLayout>
				<pattern>${sys:FILE_LOG_PATTERN}</pattern>
			</PatternLayout>
			<Policies>
				<SizeBasedTriggeringPolicy size="20MB" />
			</Policies>
			<Filters>
				<ThresholdFilter level="warn" onMatch="deny"
					onMismatch="neutral" />
				<ThresholdFilter level="info" onMatch="accept"
					onMismatch="deny" />
			</Filters>
		</RollingRandomAccessFile>

		<!-- WARN级别日志 -->
		<RollingRandomAccessFile name="warn_appender"
								 immediateFlush="false"
								 fileName="${LOG.PATH}/${PROJECT.NAME}-warn.log"
			filePattern="${LOG.PATH}/${PROJECT.NAME}/warn/${PROJECT.NAME}-warn - %d{yyyy-MM-dd HH_mm_ss}.log.gz">
			<PatternLayout>
				<pattern>${sys:FILE_LOG_PATTERN}</pattern>
			</PatternLayout>
			<Policies>
				<SizeBasedTriggeringPolicy size="10MB" />
			</Policies>
			<Filters>
				<ThresholdFilter level="error" onMatch="deny"
					onMismatch="neutral" />
				<ThresholdFilter level="warn" onMatch="accept"
					onMismatch="deny" />
			</Filters>
		</RollingRandomAccessFile>

		<!-- ERROR级别日志 -->
		<RollingRandomAccessFile name="error_appender"
								 immediateFlush="false"
								 fileName="${LOG.PATH}/${PROJECT.NAME}-error.log"
			filePattern="${LOG.PATH}/${PROJECT.NAME}/error/${PROJECT.NAME}-error - %d{yyyy-MM-dd HH_mm_ss}.log.gz">
			<PatternLayout>
				<pattern>${sys:FILE_LOG_PATTERN}</pattern>
			</PatternLayout>
			<Policies>
				<SizeBasedTriggeringPolicy size="2MB" />
			</Policies>
			<Filters>
				<ThresholdFilter level="error" onMatch="accept"
					onMismatch="deny" />
			</Filters>
		</RollingRandomAccessFile>

		<!--用于配置慢SQL的输出 -->
<!--		<RollingFile name="slowSQL"-->
<!--			fileName="${LOG.PATH}/${PROJECT.NAME}-slowSQL.log" append="true"-->
<!--			filePattern="${LOG.PATH}/${PROJECT.NAME}/slowSQL/${PROJECT.NAME}-slowSQL-%d{yyyy-MM-dd}-%i.log">-->
<!--			<ThresholdFilter level="error" />-->
<!--			<PatternLayout pattern="${sys:FILE_LOG_PATTERN}" />-->
<!--			<Policies>-->
				<!-- 归档每天的文件 -->
<!--				<TimeBasedTriggeringPolicy interval="1"-->
<!--					modulate="true" />-->
				<!-- 限制单个文件大小 -->
<!--				<SizeBasedTriggeringPolicy size="10 MB" />-->
<!--			</Policies>-->
<!--		</RollingFile>-->

		<!-- CSRF过滤器输出日志 -->
<!--		<RollingFile name="csrfLogger"-->
<!--			fileName="${LOG.PATH}/${PROJECT.NAME}-csrfLogger.log" append="true"-->
<!--			filePattern="${LOG.PATH}/${PROJECT.NAME}/csrfLogger/${PROJECT.NAME}-csrfLogger-%d{yyyy-MM-dd}-%i.log">-->
<!--			<ThresholdFilter level="error" />-->
<!--			<PatternLayout pattern="${sys:FILE_LOG_PATTERN}" />-->
<!--			<Policies>-->
				<!-- 归档每天的文件 -->
<!--				<TimeBasedTriggeringPolicy interval="1"-->
<!--					modulate="true" />-->
				<!-- 限制单个文件大小 -->
<!--				<SizeBasedTriggeringPolicy size="10 MB" />-->
<!--			</Policies>-->
<!--		</RollingFile>-->

		<!-- 定时任务过滤器 -->
<!--		<RollingFile name="jobLogger"-->
<!--			fileName="${LOG.PATH}/${PROJECT.NAME}-jobLogger.log" append="true"-->
<!--			filePattern="${LOG.PATH}/${PROJECT.NAME}/jobLogger/${PROJECT.NAME}-jobLogger %d{yyyy-MM-dd}-%i.log">-->
<!--			<ThresholdFilter level="info" />-->
<!--			<PatternLayout-->
<!--				pattern="%5p %d{yyyy-MM-dd HH:mm:ss} (%F:%L) %m%n" />-->
<!--			<Policies>-->
				<!-- 归档每天的文件 -->
<!--				<TimeBasedTriggeringPolicy interval="1"-->
<!--					modulate="true" />-->
				<!-- 限制单个文件大小 -->
<!--				<SizeBasedTriggeringPolicy size="10 MB" />-->
<!--			</Policies>-->
<!--		</RollingFile>-->

	</Appenders>

	<!-- No need to set system property "log4j2.contextSelector" to any value
             when using <asyncLogger> or <asyncRoot>. -->
	<Loggers>
		<!-- 配置日志的根节点 -->
		<!-- pattern layout actually uses location, so we need to include it -->
		<AsyncRoot level="debug" includeLocation="true">
			<AppenderRef ref="console_out_appender" />
			<AppenderRef ref="console_err_appender" />
			<AppenderRef ref="info_appender" />
			<AppenderRef ref="warn_appender" />
			<AppenderRef ref="error_appender" />
		</AsyncRoot>

		<!-- 第三方日志系统 -->
		<AsyncLogger name="org.springframework" level="info" includeLocation="true"/>
		<logger name="org.springframework.core" level="info" />
		<logger name="org.springframework.beans" level="info" />
		<logger name="org.springframework.context" level="info" />
		<logger name="org.springframework.web" level="info" />
		<Logger name="org.jboss.netty" level="warn" />
		<Logger name="org.apache.http" level="warn" />
		<Logger name="org.jasig" level="info" />
		<logger name="org.mybatis" level="INFO" />
		<logger name="com.alibaba.druid" level="info" />
		<logger name="com.alibaba.druid" level="info" />

<!--		<Logger name="com.alibaba.druid.filter.stat" level="error" additivity="false">-->
<!--			<AppenderRef ref="slowSQL" />-->
<!--		</Logger>-->
	</Loggers>
</Configuration>
